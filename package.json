{
    "name": "etl-marketplace",
    "version": "1.0.0",
    "description": "ETL Marketplace monorepo",
    "private": true,
    "workspaces": [
        "packages/*"
    ],
    "scripts": {
        "build": "bun run --cwd packages/* build",
        "dev": "bun run --cwd packages/* dev",
        "lint": "oxlint './packages/**/*.{ts,tsx}' . --fix",
        "test": "bun run --cwd packages/* test"
    },
    "author": "",
    "license": "ISC",
    "devDependencies": {
        "bun-types": "latest"
    },
    "peerDependencies": {
        "typescript": "^5.0.0"
    },
    "dependencies": {
        "oxlint": "^1.11.2",
        "prettier": "^3.6.2"
    }
}
